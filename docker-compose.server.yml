name: publictext-server

services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-public_text}
      POSTGRES_USER: ${POSTGRES_USER:-postgres}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-Postgres1234}
    volumes:
      - ./data/postgres:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U $$POSTGRES_USER -d $$POSTGRES_DB"]
      interval: 5s
      timeout: 3s
      retries: 20
    restart: unless-stopped

  backend:
    build:
      context: ./backend
    depends_on:
      postgres:
        condition: service_healthy
    environment:
      DATABASE_URL: postgresql+psycopg2://${POSTGRES_USER:-postgres}:${POSTGRES_PASSWORD:-Postgres1234}@postgres:5432/${POSTGRES_DB:-public_text}
      CORS_ORIGINS: ${CORS_ORIGINS:-http://10.211.4.231}
      CORS_ORIGIN_REGEX: ${CORS_ORIGIN_REGEX:-^https?://(10\.211\.4\.231|localhost|127\.0\.0\.1)(?::\d+)?$}
      STORAGE_MODE: local
      EXPORT_DIR: /data/storage
      DEEPSEEK_API_KEY: ${DEEPSEEK_API_KEY:-}
      DEEPSEEK_REQUIRE_API_KEY: ${DEEPSEEK_REQUIRE_API_KEY:-false}
      DEEPSEEK_BASE_URL: ${DEEPSEEK_BASE_URL:-http://10.211.49.42:8124/v1/models}
      DEEPSEEK_MODEL: ${DEEPSEEK_MODEL:-deepseek-chat}
      DEEPSEEK_TIMEOUT_SEC: ${DEEPSEEK_TIMEOUT_SEC:-45}
      DEEPSEEK_TEMPERATURE: ${DEEPSEEK_TEMPERATURE:-0.2}
      TEMPLATE_INFERENCE_ENGINE: ${TEMPLATE_INFERENCE_ENGINE:-hybrid}
      TEMPLATE_INFERENCE_AI_MAX_PARAGRAPHS: ${TEMPLATE_INFERENCE_AI_MAX_PARAGRAPHS:-80}
    volumes:
      - ./data/storage:/data/storage
    ports:
      - "127.0.0.1:${BACKEND_PORT:-8000}:8000"
    restart: unless-stopped

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile.prod
      args:
        VITE_API_BASE: /api
        VITE_ASSET_BASE: ""
    depends_on:
      backend:
        condition: service_started
    ports:
      - "${APP_PORT:-80}:80"
    restart: unless-stopped
